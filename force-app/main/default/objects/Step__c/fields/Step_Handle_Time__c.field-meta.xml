<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Step_Handle_Time__c</fullName>
    <externalId>false</externalId>
    <formula>IF( AND (Terminal_Stage__c, ISBLANK( Next_Step__c )) ,
null,
IF(NOT(ISBLANK(EndingTS__c )),
	EndingTS__c - InitialTS__c,
	IF(NOT(ISBLANK(InitialTS__c)),
		NOW() - InitialTS__c,
		NOW() - CreatedDate
	)
))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Handle Time (Days in this Step)</label>
    <precision>18</precision>
    <required>false</required>
    <scale>1</scale>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
